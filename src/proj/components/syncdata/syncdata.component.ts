import {Component} from '@angular/core';
import {GetTemplate} from '../../services/Utility/pathUtil';

@Component({
    templateUrl: GetTemplate("syncdata", "syncdata.html")
})
export class SyncdataComponent {
}